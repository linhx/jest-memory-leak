{
  "name": "jestleak",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --logHeapUsage --runInBand --detectLeaks",
    "testc": "cross-env NODE_OPTIONS=--max-old-space-size=60 jest --logHeapUsage --runInBand  --coverage"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.6.3"
  },
  "dependencies": {
    "cross-env": "^7.0.3"
  }
}
